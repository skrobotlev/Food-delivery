import React, { useState, ReactNode } from "react";
// import { Button } from "./tab-test-button";
// import "./styles.css";

// const buttonsList = ["Hello", "World", "Buttons"];
// const contentList = ["World", "Buttons"];



// const Tab = () => {
//     const [toggleState, setToggleState] = useState(0);

//     const updateIndex = (index) => {
//         setToggleState(index);
//     };
//     return (
//         <div>
//             {buttonsList.map((text, i) => (
//                 <Button
//                     key={i}
//                     className={toggleState === i && "active"}
//                     onClick={() => updateIndex(i)}
//                 >
//                     {text}
//                 </Button>
//             ))}
//             {contentList.map((element, i) => (
//                 <div key={i}>{toggleState === i && element}</div>
//             ))}
//         </div>
//     );
// };

// export { Tab };
